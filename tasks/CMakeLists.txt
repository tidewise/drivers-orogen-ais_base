# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(ais_baseTaskLib)
ADD_LIBRARY(${AIS_BASE_TASKLIB_NAME} SHARED
    ${AIS_BASE_TASKLIB_SOURCES})
add_dependencies(${AIS_BASE_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${AIS_BASE_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${AIS_BASE_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${AIS_BASE_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${AIS_BASE_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${AIS_BASE_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${AIS_BASE_TASKLIB_INTERFACE_LIBRARIES}")

orogen_apply_task_library_options(${AIS_BASE_TASKLIB_NAME})

INSTALL(TARGETS ${AIS_BASE_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${AIS_BASE_TASKLIB_HEADERS}
    DESTINATION include/orocos/ais_base)
